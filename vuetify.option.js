import colors from "vuetify/es5/util/colors";

export default {
  // to do
  theme: {
    dark: true,
    themes: {
      dark: {
        primary: colors.red.base,
        test: colors.purple.base
      },
    }
  }
};
